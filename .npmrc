# Ensure proper platform handling for deployment
target_platform=linux
target_arch=x64
target_libc=glibc

# Use npm ci for faster, reliable builds
engine-strict=true

# Disable optional dependencies that might cause platform issues
optional=false
